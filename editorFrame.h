#pragma once

#include <wx/wx.h>

class EditorFrame : public wxDialog
{
public:
	EditorFrame(wxWindow* parent, wxWindowID id, const wxString& title, const wxPoint& pos, const wxSize& size, long style, const wxString& name);

private:

};